import { PrimepricePipe } from '../shared/primeprice.pipe';

describe('PrimepricePipe', () => {
  it('create an instance', () => {
    const pipe = new PrimepricePipe();
    expect(pipe).toBeTruthy();
  });
});
